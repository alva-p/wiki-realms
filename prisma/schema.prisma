generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model users {
  user_id       BigInt  @id
  total_points  BigInt? @default(0)
  last_point_at BigInt?
}
